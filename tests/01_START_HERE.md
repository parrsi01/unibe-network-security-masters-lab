# Tests Start Here

Run repository tests with:

```bash
python3 -m unittest discover -s tests -p 'test_*.py' -v
```

The current test suite validates the Suricata EVE sample triage parser and summary logic.

