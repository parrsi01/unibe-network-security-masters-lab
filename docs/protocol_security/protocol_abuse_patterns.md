# Protocol Abuse Patterns

Study how attackers misuse protocol semantics rather than only exploiting software bugs:

- DNS tunneling and covert signaling
- HTTP header anomalies and staged delivery
- TLS SNI / JA3 metadata misuse patterns
- scan behavior visible in TCP handshake and timing patterns

