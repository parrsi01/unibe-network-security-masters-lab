# Troubleshooting Tree

- No output:
  - Confirm the file path exists
  - Confirm Python 3 is installed
- Incorrect counts:
  - Validate JSONL formatting
  - Check that lines are not duplicated
- Missing alerts:
  - Inspect `event_type` fields in the sample
